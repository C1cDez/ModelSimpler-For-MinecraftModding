{
    "parent": "minecraft:block/#{variant}stairs",
    "textures": {
        "bottom": "#{modId}:#{blockOrBlocks}/#{id}",
        "top": "#{modId}:#{blockOrBlocks}/#{id}",
        "side": "#{modId}:#{blockOrBlocks}/#{id}"
    }
}
